{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/backend/Dockerfile.worker"
  },
  "deploy": {
    "healthcheckPath": null,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "arq app.workers.apply_worker.ApplyWorkerSettings"
  }
}
