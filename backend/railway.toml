# Railway config-as-code for the FastAPI API service.
# Set the service Root Directory to `backend`, or set custom config path to `/backend/railway.toml`.
# Reference: https://docs.railway.app/reference/config-as-code

[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
